# Basic Redis configuration for Coolify
bind 0.0.0.0
protected-mode no

# Memory settings
maxmemory 256mb
maxmemory-policy allkeys-lru

# Disable persistence to prevent issues
save ""
appendonly no

# Performance settings
tcp-keepalive 300
timeout 0 